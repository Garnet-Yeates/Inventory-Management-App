// Used for creating new Item Types. No way to edit 'in-progress' ones since creation is very simple

const CreateItemTypePage = (props) => {

    return (
        <div>
            Create Item Type Page
        </div>
    )

}

export default CreateItemTypePage;